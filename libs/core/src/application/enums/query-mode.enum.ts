// import { registerEnumType } from '@nestjs/graphql';

export enum EQueryMode {
  DEFAULT = 'default',
  INSENSITIVE = 'insensitive',
}

// registerEnumType(EQueryMode, {
//   name: 'QueryMode',
// });
