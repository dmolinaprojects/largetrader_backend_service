import { ConsumerCacheInterceptor } from './consumer-cache.interceptor';

describe('ConsumerCacheInterceptor', () => {
  it('should be defined', () => {
    expect(new ConsumerCacheInterceptor()).toBeDefined();
  });
});
